# GeminiTranslator

This extension will allow a user to define a language other than the default, and then navigate the website in that language, with Gemini dynamically translating any requested articles on the fly. 
